// Requires mocha
var assert = require('assert');

describe('Test #1', function () {
  describe('Test that our data is equal', function () {
    it('Should return true if equal and false if unequal', function () {
      assert.equal(4, 4);
    });
  });
});

